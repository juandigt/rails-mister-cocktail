<h1> Cocktail:</h1>

<h3><%= @cocktail.name %></h3>

<ul><% @cocktail.doses.each do |dose| %>
      <li>
        <h3><%=  dose.ingredient.name %> --- <%= dose.description  %>
        <%= link_to "Delete Dose", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %></h3>
      </li>
    <% end %>
</ul>


--- <%= link_to "add new dose", new_cocktail_dose_path(@cocktail) %>

<br>

----<%= link_to "back", cocktails_path %>

<br>
<%= link_to "Delete Cocktail", cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" } %>

